# Cloudflare Pages redirects config.
# Defensive rule: guarantee /robots.txt always resolves to the static file
# in the build output (never falls through to the SPA/HTML fallback).
# The 200 status keeps it a rewrite, not a redirect.
/robots.txt  /robots.txt  200
