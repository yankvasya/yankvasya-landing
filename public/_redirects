# Cloudflare Pages redirects config.
# NOTE: No rule for /robots.txt here. The static file in the build output is
# served directly by Cloudflare Pages. A self-referencing rewrite
# (/robots.txt -> /robots.txt) can loop and cause "unable to download
# robots.txt". Rely on _headers for the correct Content-Type instead.
